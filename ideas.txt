Project szintű ötletek:
- product js cdn-ről érkezik mindehova, így updatelhető könnyen


Frontend:
- undo feature
- game (preset) selection -> displays the game's placeholders in template selection and offers options for images
- szabályzat implementáció


Backend:
+ ! webpage basic data patch nem működik
+ template és preset id 
            --refactored ami eddig presetId- mostmar templateId
            --presetId felesleges
+ szabályzat model
            type RulesModel struct {
                Id    int      `json:"id"`
                Rules string `json:"rules"`
            }

+ navigation itemekhez hozzávenni egy enabled booleant
            type NavItem struct {
                Id   int    `json:"id"`
                Name string `json:"name"`
                Path string `json:"path"`
                Enabled bool `json:"enabled"`
            }
